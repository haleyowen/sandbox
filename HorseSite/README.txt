1. Install gems:

bundle install


2. Start app server:

padrino start
or
bundle exec padrino start

Navigate to localhost:3000


3. Migrate database:

rake dm:migrate


4. Seed database:

padrino rake seed
or
bundle exec padrino rake seed
